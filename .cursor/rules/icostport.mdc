---
description: iCostPort 项目约定 — 代码注释与文档字符串使用中文
globs: "**/*.py"
alwaysApply: true
---

# iCostPort 项目规则

## 注释语言

- 所有 **Python 注释**（`#` 行注释、块说明）使用 **简体中文**。
- 所有 **文档字符串**（模块、类、函数的 docstring）使用 **简体中文**。
- 已有英文注释在修改相关代码时，改为中文或补充中文说明，保持与周围一致。

## 代码标识符

- **标识符**（模块名、变量、函数、类名等）保持项目既有风格；与标准库或第三方 API 一致的名称保持英文，不强行翻译。

## 例外

- 法律许可头、必须从上游保留的英文注释、或工具强制生成的片段可保留原样，并在旁用中文说明（如需要）。
